/** @type {import('next').NextConfig} */
const nextConfig = {
    trailingSlash: true,
    basePath: '/do-not-cut-the-rope',
    assetPrefix: '/do-not-cut-the-rope/',
    output: 'export'
};

export default nextConfig;
